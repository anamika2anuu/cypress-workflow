{
  "name": "workflow",
  "version": "1.0.0",
  "scripts": {
    "test:unit": "jest",
    "test:e2e": "cypress run --headless --browser chrome",
    "test": "npm run test:unit && npm run test:e2e"
  },
  "devDependencies": {
    "cypress": "^15.3.0",
    "jest": "^29.0.0"
  }
}
